#cmake_minimum_required (VERSION 2.6)
project (GenerousTracker)
set (GenerousTracker_VERSION_MAJOR 1)
set (GenerousTracker_VERSION MINOR 0)
find_package(Qt5Core)
find_package(Curses)
add_executable(donate main.cpp parser.cpp filter.cpp tracker.cpp project.cpp payment.cpp printer.cpp)
target_link_libraries(donate Qt5::Core ${CURSES_LIBRARIES})
